# Session du 28/08/2026

## Cadrage initial (clôturé à 17:05)

Pas de phase PRD à clôturer : session `/start`, le PRD n'existe pas encore.

### Commits
- `17cfc32` chore(setup): importe le kit Claude Code comme base du projet
- `9f9443c` docs(cadrage): cadre Carneo, identité projet et décisions d'architecture

### Fichiers produits
- `CLAUDE.md` : section Identité, `project_type: webapp`, `project_uses_n8n: false`
- `STATUS.md` : initialisé au nom du projet
- `projet-carnet-entretien.md` : section 10 "Décisions arrêtées au 28/08/2026" ajoutée

### Décisions prises
- Projet personnel sans visée commerciale, mais montrable en portfolio. Impose un compte de démonstration à données fictives avant toute mise en avant publique.
- La frontière de l'historique fiable est l'achat de 2018 à 56 000 km. Aucune table supplémentaire : `purchase_date` et `purchase_mileage` suffisent, le calcul rend trois états au lieu de deux (le troisième étant "inconnu, à faire vérifier", cas de la courroie de distribution).
- Contrôle technique intégré au suivi, en date pure (24 mois), modélisé comme `plan_operations` avec une catégorie `réglementaire`.
- Deux champs ajoutés sur `vehicles` : `first_registration_date` (le `year` entier ne suffit pas pour le CT) et `purchase_price` (sans lui la dépréciation est hors de portée).
- Logique d'échéances en fonction TypeScript pure sans accès base, pas une vue SQL, pour la rendre testable et réutilisable par le cron de l'étape 6.
- v3 parquée en bloc (coût de possession, carburant, argus, OCR, analyse de devis). Critère de tri unique pour la v1 : est-ce que ça sert l'écran des prochaines échéances ?

### Outillage
- Repo git initialisé sur `main`, identité héritée du global (`davhoa974`, email GitHub noreply)
- Playwright MCP installé et connecté
- n8n non retenu sur ce projet
- `CLAUDE.md` retiré du `.gitignore` du kit (projet, pas kit à redistribuer)

### Points laissés à `/architect`
Seuil de fiabilité de la projection kilométrique, correction et suppression des relevés (compteur qui recule), nature du champ `cost` (TTC ou HT), canal de notification pour l'étape 6.

---

## `/architect` : cadrage et scaffold (clôturé à 20:2x)

### Commits
- `da56ee8` docs(prd): cadre le périmètre V1 de Carneo en 8 phases
- `29d4e0c` chore(scaffold): initialise Next.js 16 avec Supabase

### Décisions prises pendant la session
- Niveau Request Classification : **FULL** (6 phases de code, RLS partout, coeur métier testé, objectif portfolio).
- Les quatre points laissés ouverts par `/start` sont tranchés : projection kilométrique à partir de 3 relevés couvrant 60 jours (fenêtre glissante 12 mois, sinon km restants sans date) ; `cost` en TTC global pièces et main d'oeuvre confondues ; rappels par email derrière une interface d'envoi remplaçable ; correction et suppression des relevés au scope V1 avec alerte quand le compteur recule.
- **Écart assumé avec le brief** : l'extraction des factures par photo passe de la v2 (§10.6 la parquait) à la V1, en Phase 5. Demande explicite de David, qui refuse de ressaisir huit ans d'historique.
- La photo pré-remplit le formulaire, elle n'écrit jamais en base. Une seule voie d'écriture. Tracé en ADR-002.

### Trou trouvé dans le modèle de données du brief
Le §4 posait `documents.maintenance_event_id` : une facture pointe vers une intervention. Or une visite au garage produit une facture mais plusieurs opérations (vidange + filtre + plaquettes). Lien inversé en `maintenance_events.document_id`. Correction à trois lignes aujourd'hui, migration douloureuse dans six mois.

### Gotcha outillage
`create-next-app` refuse de tourner dans un dossier non vide : il ne tolère que `.git`, `.gitignore`, `README.md`, `LICENSE` et `docs`. Le kit occupait la racine avec 19 fichiers. Solution retenue : scaffold dans `/tmp` puis rapatriement en excluant `README.md`, `.gitignore`, `AGENTS.md` et surtout `CLAUDE.md`, que create-next-app génère aussi (un pointeur de 11 octets vers `AGENTS.md`) et qui aurait écrasé le CLAUDE.md du projet. À refaire ainsi si le scaffold est rejoué.

### Vérifications réellement effectuées
`npm run build` vert, TypeScript strict compilé, serveur de dev en HTTP 200, `.env` confirmé gitignored via `git check-ignore`, connexion Supabase testée par requête réelle (PostgREST répond `PGRST205` avec la clé publique, `auth.users` accessible avec la clé secrète, 0 utilisateur).

### Reste ouvert
Le Prérequis hors code : extraire le plan d'entretien du carnet constructeur de la Fiesta. Bloque la Phase 2, pas la Phase 1.

## Phase 1 — Squelette technique (clôturée à 22:15)

### Commits
- `29d5c3c` chore(phase-1): cloture la Phase 1 validee en production *(commit de cette /close)*
- `1071ed8` chore: commit vide pour verifier le deploiement automatique
- `b2ad6e2` chore(plan): coche la tache 4 et acte le depot public
- `f356b87` docs(plan): corrige le diagnostic sur le MCP Playwright
- `352a972` chore(plan): coche les trois premieres taches de la Phase 1
- `3585936` chore: aligne next-env.d.ts sur la sortie de next build
- `1e9c80c` feat(ui): remplace le template Next.js par la page de sante
- `4704e15` feat(health): expose une route de sante qui interroge Supabase
- `884a84d` feat(supabase): ajoute la garde d'environnement et les deux clients

### Fichiers modifiés
16 fichiers, 291 insertions, 76 suppressions. Principaux : `src/lib/env.ts`, `src/lib/supabase/{client,server,health}.ts`, `src/app/api/health/route.ts`, `src/app/page.tsx`, `docs/plans/phase-1-plan.md`, `PRD.md`, `STATUS.md`.

### Durée approximative
Du premier commit de la phase à la clôture : environ 5 heures sur la journée du 28/08/2026.

### Ce que la phase a coûté en dehors du code
La moitié du temps est passée hors éditeur : création du dépôt GitHub, liaison Vercel, et surtout deux allers-retours sur les variables d'environnement Vercel (type Secret verrouillé à l'enregistrement, puis variables vides). Détail technique consigné dans les Découvertes de `docs/plans/phase-1-plan.md`.
