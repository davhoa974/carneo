# Session du 28/08/2026

## Cadrage initial (clôturé à 17:05)

Pas de phase PRD à clôturer : session `/start`, le PRD n'existe pas encore.

### Commits
- `17cfc32` chore(setup): importe le kit Claude Code comme base du projet
- `9f9443c` docs(cadrage): cadre Carneo, identité projet et décisions d'architecture

### Fichiers produits
- `CLAUDE.md` : section Identité, `project_type: webapp`, `project_uses_n8n: false`
- `STATUS.md` : initialisé au nom du projet
- `projet-carnet-entretien.md` : section 10 "Décisions arrêtées au 28/08/2026" ajoutée

### Décisions prises
- Projet personnel sans visée commerciale, mais montrable en portfolio. Impose un compte de démonstration à données fictives avant toute mise en avant publique.
- La frontière de l'historique fiable est l'achat de 2018 à 56 000 km. Aucune table supplémentaire : `purchase_date` et `purchase_mileage` suffisent, le calcul rend trois états au lieu de deux (le troisième étant "inconnu, à faire vérifier", cas de la courroie de distribution).
- Contrôle technique intégré au suivi, en date pure (24 mois), modélisé comme `plan_operations` avec une catégorie `réglementaire`.
- Deux champs ajoutés sur `vehicles` : `first_registration_date` (le `year` entier ne suffit pas pour le CT) et `purchase_price` (sans lui la dépréciation est hors de portée).
- Logique d'échéances en fonction TypeScript pure sans accès base, pas une vue SQL, pour la rendre testable et réutilisable par le cron de l'étape 6.
- v3 parquée en bloc (coût de possession, carburant, argus, OCR, analyse de devis). Critère de tri unique pour la v1 : est-ce que ça sert l'écran des prochaines échéances ?

### Outillage
- Repo git initialisé sur `main`, identité héritée du global (`davhoa974`, email GitHub noreply)
- Playwright MCP installé et connecté
- n8n non retenu sur ce projet
- `CLAUDE.md` retiré du `.gitignore` du kit (projet, pas kit à redistribuer)

### Points laissés à `/architect`
Seuil de fiabilité de la projection kilométrique, correction et suppression des relevés (compteur qui recule), nature du champ `cost` (TTC ou HT), canal de notification pour l'étape 6.
